@model Dictionary<string, object>

<div>
    <h4>User Details</h4>
</div>

<table>
    @foreach (var kvp in Model) {
        <tr>
            <th>@kvp.Key</th>
            <td>@kvp.Value</td>
        </tr>
    }
</table>