@model SportsStore.Models.ViewModels.ProductListViewModel

@foreach (var product in Model.Products) {
    <partial name="ProductSummary" model="@product"/>
}

<div page-model="@Model.PagingInfo" page-action="List" page-url-category="@Model.CurrentCategory"></div>